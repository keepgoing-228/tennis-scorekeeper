# i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add multi-language support (English, Chinese, Japanese) to the Tennis Scorekeeper PWA using react-i18next.

**Architecture:** Install react-i18next with browser language detector. Create one JSON translation file per language. Replace all hardcoded UI strings with `t()` calls. Add a segmented pill language switcher at the bottom of the NewMatch page.

**Tech Stack:** react-i18next, i18next, i18next-browser-languagedetector

---

## File Structure

| Action | Path | Responsibility |
|--------|------|---------------|
| Create | `src/i18n/index.ts` | i18next initialization, language detection config, resource registration |
| Create | `src/i18n/locales/en.json` | English translations (~60 keys) |
| Create | `src/i18n/locales/zh.json` | Chinese translations |
| Create | `src/i18n/locales/ja.json` | Japanese translations |
| Create | `src/ui/components/LanguageSwitcher.tsx` | Segmented pill toggle (EN / 中文 / 日本語) |
| Modify | `src/main.tsx` | Import `src/i18n/index.ts` |
| Modify | `src/ui/pages/NewMatch.tsx` | Replace hardcoded strings with `t()`, add LanguageSwitcher |
| Modify | `src/ui/pages/Scoring.tsx` | Replace hardcoded strings with `t()` |
| Modify | `src/ui/pages/MatchHistory.tsx` | Replace hardcoded strings with `t()`, convert STAT_LABELS |
| Modify | `src/ui/components/Scoreboard.tsx` | Replace hardcoded strings with `t()` |
| Modify | `src/ui/components/AnnotationBar.tsx` | Convert constant arrays to use `labelKey`, accept `t` prop or use hook |

---

### Task 1: Install dependencies

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Install i18next packages**

Run:
```bash
npm install i18next react-i18next i18next-browser-languagedetector
```

- [ ] **Step 2: Verify installation**

Run:
```bash
npm ls i18next react-i18next i18next-browser-languagedetector
```
Expected: All three packages listed with version numbers, no errors.

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: install i18next, react-i18next, i18next-browser-languagedetector"
```

---

### Task 2: Create English translation file and i18next config

**Files:**
- Create: `src/i18n/locales/en.json`
- Create: `src/i18n/index.ts`
- Modify: `src/main.tsx`

- [ ] **Step 1: Create the English translation file**

Create `src/i18n/locales/en.json`:

```json
{
  "appTitle": "Tennis Scorekeeper",
  "teamA": "Team A",
  "teamB": "Team B",
  "bestOf": "Best Of",
  "practice": "Practice",
  "practiceMode": "Practice Mode",
  "tiebreak": "Tiebreak",
  "firstTo3": "First to 3",
  "tiebreakNone": "None",
  "tiebreak7pt": "7-point",
  "matchType": "Match Type",
  "singles": "Singles",
  "doubles": "Doubles",
  "firstServer": "First Server",
  "startMatch": "Start Match",
  "matchHistory": "Match History",
  "loading": "Loading...",
  "matchNotFound": "Match not found",
  "teamWins": "{{teamName}} wins!",
  "winnerSummary": "{{winnerName}} wins",
  "confirmCancel": "Cancel this match and start a new one?",
  "confirmDeleteAll": "Delete all matches? This cannot be undone.",
  "confirmDelete": "Delete this match? This cannot be undone.",
  "restart": "Restart",
  "undo": "↩ Undo",
  "newMatch": "New Match",
  "noMatches": "No completed matches yet.",
  "deleteAll": "Delete All Matches",
  "vs": "vs",
  "tiebreakIndicator": "TIEBREAK",
  "firstTo3Games": "First to 3 Games",
  "practiceTiebreak": "Practice Tiebreak",
  "bestOfN": "Best of {{n}}",
  "ace": "Ace",
  "winner": "Winner",
  "doubleFault": "Double Fault",
  "fhError": "FH Error",
  "bhError": "BH Error",
  "volleyError": "Volley",
  "outError": "Out",
  "netError": "Net",
  "statTotalPoints": "Total Points Won",
  "statAces": "Aces",
  "statDoubleFaults": "Double Faults",
  "statForehandErrors": "Forehand Errors",
  "statBackhandErrors": "Backhand Errors",
  "statVolleyErrors": "Volley Errors",
  "statOutOfBounds": "Out of Bounds",
  "statNetErrors": "Net Errors",
  "statWinners": "Winners",
  "statUnannotated": "Unannotated",
  "deleteMatch": "Delete match"
}
```

- [ ] **Step 2: Create i18next configuration**

Create `src/i18n/index.ts`:

```ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import LanguageDetector from "i18next-browser-languagedetector";
import en from "./locales/en.json";

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { translation: en },
    },
    fallbackLng: "en",
    detection: {
      order: ["localStorage", "navigator"],
    },
    interpolation: {
      escapeValue: false,
    },
  });

export default i18n;
```

Note: We start with English only. Chinese and Japanese are added in Task 3.

- [ ] **Step 3: Import i18n in main.tsx**

Modify `src/main.tsx` — add `import './i18n/index.ts'` before the App import:

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './i18n/index.ts'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 4: Verify the app still builds**

Run:
```bash
npm run build
```
Expected: Build succeeds with no errors.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/ src/main.tsx
git commit -m "feat: add i18next config with English translations"
```

---

### Task 3: Add Chinese and Japanese translation files

**Files:**
- Create: `src/i18n/locales/zh.json`
- Create: `src/i18n/locales/ja.json`
- Modify: `src/i18n/index.ts`

- [ ] **Step 1: Create Chinese translation file**

Create `src/i18n/locales/zh.json`:

```json
{
  "appTitle": "网球记分员",
  "teamA": "队伍 A",
  "teamB": "队伍 B",
  "bestOf": "赛制",
  "practice": "练习",
  "practiceMode": "练习模式",
  "tiebreak": "抢七",
  "firstTo3": "先赢3局",
  "tiebreakNone": "无",
  "tiebreak7pt": "7分制",
  "matchType": "比赛类型",
  "singles": "单打",
  "doubles": "双打",
  "firstServer": "先发球",
  "startMatch": "开始比赛",
  "matchHistory": "比赛历史",
  "loading": "加载中...",
  "matchNotFound": "未找到比赛",
  "teamWins": "{{teamName}} 获胜！",
  "winnerSummary": "{{winnerName}} 获胜",
  "confirmCancel": "取消本场比赛并开始新比赛？",
  "confirmDeleteAll": "删除所有比赛？此操作无法撤销。",
  "confirmDelete": "删除此比赛？此操作无法撤销。",
  "restart": "重新开始",
  "undo": "↩ 撤销",
  "newMatch": "新比赛",
  "noMatches": "暂无已完成的比赛。",
  "deleteAll": "删除所有比赛",
  "vs": "vs",
  "tiebreakIndicator": "TIEBREAK",
  "firstTo3Games": "先赢3局",
  "practiceTiebreak": "练习抢七",
  "bestOfN": "{{n}}盘{{n}}胜",
  "ace": "Ace",
  "winner": "Winner",
  "doubleFault": "Double Fault",
  "fhError": "FH Error",
  "bhError": "BH Error",
  "volleyError": "Volley",
  "outError": "Out",
  "netError": "Net",
  "statTotalPoints": "总得分",
  "statAces": "Ace球",
  "statDoubleFaults": "双误",
  "statForehandErrors": "正手失误",
  "statBackhandErrors": "反手失误",
  "statVolleyErrors": "截击失误",
  "statOutOfBounds": "出界",
  "statNetErrors": "下网",
  "statWinners": "制胜分",
  "statUnannotated": "未标注",
  "deleteMatch": "删除比赛"
}
```

Note: Tennis scoring terms (Ace, Winner, Double Fault, FH Error, etc.) stay in English as per spec — these are the terms players use on court.

- [ ] **Step 2: Create Japanese translation file**

Create `src/i18n/locales/ja.json`:

```json
{
  "appTitle": "テニス スコアキーパー",
  "teamA": "チーム A",
  "teamB": "チーム B",
  "bestOf": "セット数",
  "practice": "練習",
  "practiceMode": "練習モード",
  "tiebreak": "タイブレーク",
  "firstTo3": "3ゲーム先取",
  "tiebreakNone": "なし",
  "tiebreak7pt": "7ポイント",
  "matchType": "試合形式",
  "singles": "シングルス",
  "doubles": "ダブルス",
  "firstServer": "最初のサーバー",
  "startMatch": "試合開始",
  "matchHistory": "試合履歴",
  "loading": "読み込み中...",
  "matchNotFound": "試合が見つかりません",
  "teamWins": "{{teamName}} の勝利！",
  "winnerSummary": "{{winnerName}} の勝利",
  "confirmCancel": "この試合をキャンセルして新しい試合を始めますか？",
  "confirmDeleteAll": "すべての試合を削除しますか？この操作は元に戻せません。",
  "confirmDelete": "この試合を削除しますか？この操作は元に戻せません。",
  "restart": "リスタート",
  "undo": "↩ 元に戻す",
  "newMatch": "新しい試合",
  "noMatches": "完了した試合はまだありません。",
  "deleteAll": "すべての試合を削除",
  "vs": "vs",
  "tiebreakIndicator": "TIEBREAK",
  "firstTo3Games": "3ゲーム先取",
  "practiceTiebreak": "練習タイブレーク",
  "bestOfN": "{{n}}セットマッチ",
  "ace": "Ace",
  "winner": "Winner",
  "doubleFault": "Double Fault",
  "fhError": "FH Error",
  "bhError": "BH Error",
  "volleyError": "Volley",
  "outError": "Out",
  "netError": "Net",
  "statTotalPoints": "合計得点",
  "statAces": "エース",
  "statDoubleFaults": "ダブルフォルト",
  "statForehandErrors": "フォアハンドエラー",
  "statBackhandErrors": "バックハンドエラー",
  "statVolleyErrors": "ボレーエラー",
  "statOutOfBounds": "アウト",
  "statNetErrors": "ネットエラー",
  "statWinners": "ウィナー",
  "statUnannotated": "未記録",
  "deleteMatch": "試合を削除"
}
```

- [ ] **Step 3: Register Chinese and Japanese in i18next config**

Update `src/i18n/index.ts`:

```ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import LanguageDetector from "i18next-browser-languagedetector";
import en from "./locales/en.json";
import zh from "./locales/zh.json";
import ja from "./locales/ja.json";

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { translation: en },
      zh: { translation: zh },
      ja: { translation: ja },
    },
    fallbackLng: "en",
    detection: {
      order: ["localStorage", "navigator"],
    },
    interpolation: {
      escapeValue: false,
    },
  });

export default i18n;
```

- [ ] **Step 4: Verify build**

Run:
```bash
npm run build
```
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/
git commit -m "feat: add Chinese and Japanese translation files"
```

---

### Task 4: Create LanguageSwitcher component

**Files:**
- Create: `src/ui/components/LanguageSwitcher.tsx`

- [ ] **Step 1: Create the LanguageSwitcher component**

Create `src/ui/components/LanguageSwitcher.tsx`:

```tsx
import { useTranslation } from "react-i18next";

const LANGUAGES = [
  { code: "en", label: "EN" },
  { code: "zh", label: "中文" },
  { code: "ja", label: "日本語" },
];

export default function LanguageSwitcher() {
  const { i18n } = useTranslation();

  return (
    <div className="flex justify-center">
      <div className="inline-flex bg-gray-800 rounded-full p-0.5">
        {LANGUAGES.map(({ code, label }) => (
          <button
            key={code}
            type="button"
            onClick={() => i18n.changeLanguage(code)}
            className={`px-4 py-1.5 text-xs font-medium rounded-full transition-colors duration-150 ${
              i18n.resolvedLanguage === code
                ? "bg-blue-600 text-white"
                : "text-gray-400 hover:text-gray-300"
            }`}
          >
            {label}
          </button>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify build**

Run:
```bash
npm run build
```
Expected: Build succeeds (component not yet used, just verifying no syntax errors).

- [ ] **Step 3: Commit**

```bash
git add src/ui/components/LanguageSwitcher.tsx
git commit -m "feat: add LanguageSwitcher pill toggle component"
```

---

### Task 5: Integrate i18n into NewMatch page

**Files:**
- Modify: `src/ui/pages/NewMatch.tsx:1-255`

- [ ] **Step 1: Add imports**

Add at the top of `NewMatch.tsx`, after existing imports:

```tsx
import { useTranslation } from "react-i18next";
import LanguageSwitcher from "../components/LanguageSwitcher.tsx";
```

- [ ] **Step 2: Add hook call**

Inside the `NewMatch` function, at the top (after `const [practiceMode, ...]`), add:

```tsx
const { t } = useTranslation();
```

- [ ] **Step 3: Replace all hardcoded strings**

Replace the following strings in the JSX:

| Line | Old | New |
|------|-----|-----|
| 66 | `Tennis Scorekeeper` | `{t('appTitle')}` |
| 72 | `Team A` (label) | `{t('teamA')}` |
| 78 | `placeholder="Team A"` | `placeholder={t('teamA')}` |
| 85 | `Team B` (label) | `{t('teamB')}` |
| 91 | `placeholder="Team B"` | `placeholder={t('teamB')}` |
| 100 | `Best Of` | `{t('bestOf')}` |
| 128 | `Practice` | `{t('practice')}` |
| 136 | `Practice Mode` | `{t('practiceMode')}` |
| 148 | `Tiebreak` | `{t('tiebreak')}` |
| 159 | `First to 3` | `{t('firstTo3')}` |
| 168 | `Tiebreak` (label) | `{t('tiebreak')}` |
| 184 | `{t === "none" ? "None" : "7-point"}` | `{tb === "none" ? t('tiebreakNone') : t('tiebreak7pt')}` |
| 193 | `Match Type` | `{t('matchType')}` |
| 209 | `{t}` (singles/doubles) | `{mt === "singles" ? t('singles') : t('doubles')}` |
| 217 | `First Server` | `{t('firstServer')}` |
| 233 | `(teamAName \|\| "Team A")` | `(teamAName \|\| t('teamA'))` |
| 233 | `(teamBName \|\| "Team B")` | `(teamBName \|\| t('teamB'))` |
| 243 | `Start Match` | `{t('startMatch')}` |
| 250 | `Match History` | `{t('matchHistory')}` |

**Important:** The tiebreak section (line 171) uses a variable named `t` which shadows the translation function. Rename the map variable from `t` to `tb`:

```tsx
{(["none", "7pt"] as const).map((tb, i) => (
```

Similarly, the match type section (line 196) uses `t` — rename to `mt`:

```tsx
{(["singles", "doubles"] as const).map((mt, i) => (
```

- [ ] **Step 4: Add LanguageSwitcher to the form**

After the `<Link to="/history">` block (line 246-251), before the closing `</form>`, add:

```tsx
<LanguageSwitcher />
```

- [ ] **Step 5: Verify build and test manually**

Run:
```bash
npm run build
```
Expected: Build succeeds.

Run `npm run dev` and visually verify the NewMatch page displays correctly in English, and the language switcher appears at the bottom.

- [ ] **Step 6: Commit**

```bash
git add src/ui/pages/NewMatch.tsx
git commit -m "feat: integrate i18n into NewMatch page with language switcher"
```

---

### Task 6: Integrate i18n into Scoring page

**Files:**
- Modify: `src/ui/pages/Scoring.tsx:1-212`

- [ ] **Step 1: Add import**

Add after existing imports:

```tsx
import { useTranslation } from "react-i18next";
```

- [ ] **Step 2: Add hook call**

Inside the `Scoring` function, after the `canUndo` memo (line 23), add:

```tsx
const { t } = useTranslation();
```

- [ ] **Step 3: Replace all hardcoded strings**

| Line | Old | New |
|------|-----|-----|
| 114 | `Loading...` | `{t('loading')}` |
| 122 | `Match not found` | `{t('matchNotFound')}` |
| 142 | `` {matchState.winner === "A" ? teamAName : teamBName} wins! `` | `{t('teamWins', { teamName: matchState.winner === "A" ? teamAName : teamBName })}` |
| 148 | `New Match` | `{t('newMatch')}` |
| 193 | `"Cancel this match and start a new one?"` | `t('confirmCancel')` |
| 200 | `Restart` | `{t('restart')}` |
| 207 | `↩ Undo` | `{t('undo')}` |

- [ ] **Step 4: Verify build**

Run:
```bash
npm run build
```
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```bash
git add src/ui/pages/Scoring.tsx
git commit -m "feat: integrate i18n into Scoring page"
```

---

### Task 7: Integrate i18n into MatchHistory page

**Files:**
- Modify: `src/ui/pages/MatchHistory.tsx:1-267`

- [ ] **Step 1: Add import**

Add after existing imports:

```tsx
import { useTranslation } from "react-i18next";
```

- [ ] **Step 2: Add hook call in MatchHistory**

Inside the `MatchHistory` function, after the state declarations (line 35), add:

```tsx
const { t } = useTranslation();
```

- [ ] **Step 3: Remove matchTypeLabel from MatchSummary and derive at render time**

The `matchTypeLabel` must be computed at render time (not inside `useEffect`) so it updates when the language changes without re-fetching all match data from IndexedDB.

First, remove `matchTypeLabel` from the `MatchSummary` type (line 9-14):

```tsx
type MatchSummary = {
  record: MatchRecord;
  setScores: string;
  winnerName: string;
};
```

Remove the `getMatchTypeLabel` function (lines 24-29) entirely.

Remove the `matchTypeLabel: getMatchTypeLabel(record)` line from the `load` function (line 60).

Keep the `useEffect` dependency array as `[]` — it only needs to run once on mount.

- [ ] **Step 4: Replace hardcoded strings in JSX**

| Line | Old | New |
|------|-----|-----|
| 86 | `"Delete all matches? This cannot be undone."` | `t('confirmDeleteAll')` |
| 98 | `"Delete this match? This cannot be undone."` | `t('confirmDelete')` |
| 116 | `Loading...` | `{t('loading')}` |
| 124 | `Match History` | `{t('matchHistory')}` |
| 128 | `No completed matches yet.` | `{t('noMatches')}` |
| 143 | `{" vs "}` | `{` ${t('vs')} `}` |
| 152 | `{winnerName} wins` | `{t('winnerSummary', { winnerName })}` |
| 154 | `{matchTypeLabel}` | Compute inline: `{record.ruleset.bestOf === "practice" ? (record.ruleset.practiceMode === "first_to_3" ? t("firstTo3Games") : t("practiceTiebreak")) : t("bestOfN", { n: record.ruleset.bestOf })}` |
| 168 | `aria-label="Delete match"` | `aria-label={t('deleteMatch')}` |
| 205 | `Delete All Matches` | `{t('deleteAll')}` |
| 212 | `New Match` | `{t('newMatch')}` |

Also update the destructured variable in the map (line 132) — remove `matchTypeLabel`:

```tsx
{matches.map(({ record, setScores, winnerName }) => (
```

- [ ] **Step 5: Convert STAT_LABELS to use translation keys**

Replace `STAT_LABELS` (lines 226-237):

```tsx
const STAT_LABELS: { key: keyof TeamStats; labelKey: string }[] = [
  { key: "totalPointsWon", labelKey: "statTotalPoints" },
  { key: "ACE", labelKey: "statAces" },
  { key: "DOUBLE_FAULT", labelKey: "statDoubleFaults" },
  { key: "FOREHAND_ERROR", labelKey: "statForehandErrors" },
  { key: "BACKHAND_ERROR", labelKey: "statBackhandErrors" },
  { key: "VOLLEY_ERROR", labelKey: "statVolleyErrors" },
  { key: "OUT_OF_BOUNDS", labelKey: "statOutOfBounds" },
  { key: "NET_ERROR", labelKey: "statNetErrors" },
  { key: "WINNER", labelKey: "statWinners" },
  { key: "unannotated", labelKey: "statUnannotated" },
];
```

- [ ] **Step 6: Update StatsDetail to use translations**

Add `useTranslation` to `StatsDetail`:

```tsx
function StatsDetail({ stats, teamAName, teamBName }: StatsDetailProps) {
  const { t } = useTranslation();
  return (
    ...
          {STAT_LABELS.map(({ key, labelKey }, i) => (
            ...
              <td className="py-1.5 text-gray-400">{t(labelKey)}</td>
            ...
          ))}
    ...
  );
}
```

- [ ] **Step 7: Verify build**

Run:
```bash
npm run build
```
Expected: Build succeeds.

- [ ] **Step 8: Commit**

```bash
git add src/ui/pages/MatchHistory.tsx
git commit -m "feat: integrate i18n into MatchHistory page"
```

---

### Task 8: Integrate i18n into Scoreboard component

**Files:**
- Modify: `src/ui/components/Scoreboard.tsx:1-57`

- [ ] **Step 1: Add import and hook**

Add import:
```tsx
import { useTranslation } from "react-i18next";
```

Inside `Scoreboard`, add:
```tsx
const { t } = useTranslation();
```

- [ ] **Step 2: Replace hardcoded strings**

| Line | Old | New |
|------|-----|-----|
| 15 | `"First to 3 Games"` | `t('firstTo3Games')` |
| 15 | `"Practice Tiebreak"` | `t('practiceTiebreak')` |
| 40 | `vs` | `{t('vs')}` |
| 52 | `TIEBREAK` | `{t('tiebreakIndicator')}` |

- [ ] **Step 3: Verify build**

Run:
```bash
npm run build
```
Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
git add src/ui/components/Scoreboard.tsx
git commit -m "feat: integrate i18n into Scoreboard component"
```

---

### Task 9: Integrate i18n into AnnotationBar component

**Files:**
- Modify: `src/ui/components/AnnotationBar.tsx:1-60`

- [ ] **Step 1: Add import and hook**

Add import:
```tsx
import { useTranslation } from "react-i18next";
```

Inside `AnnotationBar`, add:
```tsx
const { t } = useTranslation();
```

- [ ] **Step 2: Convert constant arrays to use translation keys**

Replace `WINNER_REASONS` (lines 3-6):
```tsx
const WINNER_REASONS: { value: PointLossReason; labelKey: string }[] = [
  { value: "ACE", labelKey: "ace" },
  { value: "WINNER", labelKey: "winner" },
];
```

Replace `ERROR_REASONS` (lines 8-15):
```tsx
const ERROR_REASONS: { value: PointLossReason; labelKey: string }[] = [
  { value: "DOUBLE_FAULT", labelKey: "doubleFault" },
  { value: "FOREHAND_ERROR", labelKey: "fhError" },
  { value: "BACKHAND_ERROR", labelKey: "bhError" },
  { value: "VOLLEY_ERROR", labelKey: "volleyError" },
  { value: "OUT_OF_BOUNDS", labelKey: "outError" },
  { value: "NET_ERROR", labelKey: "netError" },
];
```

- [ ] **Step 3: Update JSX to use t()**

Replace `{label}` with `{t(labelKey)}` in both map functions:

```tsx
{WINNER_REASONS.map(({ value, labelKey }) => (
  <button ...>
    {t(labelKey)}
  </button>
))}
...
{ERROR_REASONS.map(({ value, labelKey }) => (
  <button ...>
    {t(labelKey)}
  </button>
))}
```

- [ ] **Step 4: Verify build**

Run:
```bash
npm run build
```
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```bash
git add src/ui/components/AnnotationBar.tsx
git commit -m "feat: integrate i18n into AnnotationBar component"
```

---

### Task 10: Add .superpowers to .gitignore and run final verification

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Add .superpowers to .gitignore**

Append to `.gitignore`:
```
.superpowers/
```

- [ ] **Step 2: Run full build**

Run:
```bash
npm run build
```
Expected: Build succeeds with no errors or warnings.

- [ ] **Step 3: Run existing tests**

Run:
```bash
npm test
```
Expected: All existing tests pass (domain logic tests are not affected by i18n).

- [ ] **Step 4: Manual smoke test**

Run `npm run dev` and verify:
1. NewMatch page loads in browser's detected language (or English as fallback)
2. Language switcher at bottom of NewMatch toggles between EN / 中文 / 日本語
3. All labels update when switching languages
4. Start a match — Scoring page shows translated buttons (Restart, Undo) and confirmation dialogs
5. Finish a match — win message is translated
6. Match History shows translated headers, stats labels, and confirmation dialogs
7. Refresh the page — language preference is remembered (localStorage)
8. Tennis scoring terms (Love, 15, 30, 40, Deuce, AD) remain in English regardless of language

- [ ] **Step 5: Commit**

```bash
git add .gitignore
git commit -m "chore: add .superpowers to .gitignore"
```
